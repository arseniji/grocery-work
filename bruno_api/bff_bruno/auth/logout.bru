meta {
  name: logout
  type: http
  seq: 3
}

post {
  url: {{base_url}}/api/v1/auth/logout
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "refreshToken": "{{refresh_token}}"
  }
}
